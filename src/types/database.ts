/**
 * Generated Supabase types — run `npm run db:types` to refresh from your
 * local Supabase instance after applying migrations:
 *
 *   supabase gen types typescript --linked > src/types/database.ts
 *
 * Until then, this permissive placeholder lets the project type-check
 * while still typing the client through the generic. Once you generate
 * the real schema types, this file is overwritten with strict typings.
 *
 * NOTE: a few legacy files (funnel/engine.ts, store/context.ts, rbac/check.ts,
 * webhooks/stripe/route.ts) were written assuming `any`. Run strict
 * generation in a dedicated cleanup pass after those are tightened.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
